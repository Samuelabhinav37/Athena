# Infrastructure

Local development infrastructure is defined in the repository-level `compose.yaml`.
Terraform and deployment definitions will be introduced after the controlled identity lab is stable.
