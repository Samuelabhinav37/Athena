# Audit follow-up: 2026-09-06

Scope: working tree based on `7bd2234` on `feat/analyst-command-center`.
Changes are uncommitted. This record supersedes the verification gaps in the earlier
workflow baseline only where fresh evidence is listed below.

## Fixed and verified

1. Both Compose OPA commands now load `policies/security`. Previously, querying the
   running demo returned no `agent_actions` package. The current ingestion route
   requires that package for override events and maps unavailable decisions to HTTP 503.
   The demo OPA container was recreated, and the package is now present.
2. CI runs all three Rego directories and calls `scripts/check-policy-runtime.py`
   against the deployed OPA service. This checks a justified override, an unjustified
   override, and an unsupported destructive action with their exact expected decisions.
   Testing files alone would not catch an omitted runtime policy directory.
3. API and web images were rebuilt from current source. The old API image used the
   expected virtual-environment interpreter but lacked Azure dependencies. The rebuilt
   API successfully imports the CLI and runs the gate. CI now smoke-tests CLI imports
   in its built API image. No dependency declarations or lockfiles changed.
4. The disposable Compose runner now matches CI's ordering: backfill first, tenant
   constraints second, then the remaining suite. The former order left assigned identity
   and group rows, correctly causing the backfill precondition to reject the test state.
   Production backfill safeguards were not changed.

## Observed validation

| Check | Result |
|---|---|
| API Docker build using hashed requirements | Pass |
| Web Docker build, including TypeScript and Vite | Pass |
| API image CLI import smoke test | Pass |
| Ruff and `git diff --check` | Pass |
| Both application Compose configurations | Valid; non-secret placeholders used only for validation |
| Rego tests | 11 passed |
| Deployed agent-action policy checks | 3 passed |
| Current-source deterministic security gate | 4 fixtures, 3 controls, zero failures |
| Fresh PostgreSQL migrations | Applied through `20260828_23` |
| Alembic schema drift | No new upgrade operations detected |
| PostgreSQL backfill test | 1 passed |
| PostgreSQL tenant-constraint tests | 7 passed |
| Remaining Python suite | 281 passed |

The final database run used the actual corrected Compose test command with a local
override selecting the freshly built API image and root inside the disposable test
container for development dependency installation. Pulling the standalone Python test
image failed twice with a Docker registry token endpoint EOF. The local-image override
avoided that pull; it did not replace or weaken any test.

Warnings: Starlette/httpx deprecation and pytest cache writes rejected by the read-only
repository mount. Neither caused a test failure. No tests were skipped in the final run.

Local evidence:

- `artifacts/audit-20260906/postgres-verification.log`
- `artifacts/audit-20260906/test-image.override.yaml`
- `artifacts/audit-20260906/security-gate/security-gate-report.json`
- `artifacts/audit-20260906/security-gate/security-gate-report.md`

Built image IDs:

- API: `sha256:81bd95a1435153fdea4d1adc09bbbb45ebc6cad097abd4a37869028ef9dbb3d2`
- Web: `sha256:84d5e036e5215fd8a6b13b7935238911c551bc8b975a47c05f852a0755a470e1`

The audit PostgreSQL containers are stopped. Their dedicated volumes are retained;
the existing demo PostgreSQL volume was not migrated, reset, or deleted.

## Remaining work

The demo API and web containers still use their previous images. Compose recreation
requires `NEO4J_PASSWORD`, which was missing during this session. An operator must set
it locally to the existing Neo4j credential; credentials must not be pasted into chat.
After that, recreate only API and web from the built images and verify readiness.

Browser initialization succeeded, but selection and discovery found no connected
browser. The authenticated analyst journey remains unverified: sign in, select an
identity, trace access evidence, inspect a review and its history, and verify a report.
No UI success is inferred from container health or the passing web build.

Next delivery order:

1. Finish demo image rollout and the authenticated analyst journey.
2. Define and deliver one complete access-request and approval workflow.
3. Validate connector semantics against explicit nested/inherited/deny fixtures.
4. Implement durable telemetry and separately authorized remediation adapters as
   individually scoped workstreams.
5. Close production operational evidence in `governance/readiness.json` with approved
   ownership, deployment parameters, recovery exercises, observability, and scale evidence.

Production readiness remains blocked. These local checks are not production approval,
a backup restore rehearsal, a supply-chain audit, or a hosted CI result.
